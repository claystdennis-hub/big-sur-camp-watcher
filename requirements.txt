# Ventana (Hyatt SJCAC) monitor — runs on your Mac.
# Hyatt uses Kasada anti-bot, which blocks headless browsers and cold requests,
# so we drive a real (off-screen) browser and intercept its API response.
playwright
requests
python-dotenv

# After installing: playwright install chromium
# (The camply government-site scans run in GitHub Actions and install camply there.)
